# Recommendation research protocol V5

**Status: `closed_input_unavailable`.** V5 selected Gate.io's public Spot `BTC_USDT` endpoint
only for a mechanical availability audit of `[2019-01-01T00:00:00Z, 2022-01-01T00:00:00Z)`.
The first fixed-range request was rejected by the provider with HTTP 400:
`Candlestick too long ago. Maximum 10000 points ago are allowed`.

The source therefore cannot provide the preregistered three-year, 26,304-candle input. This is an
input-availability result—not a strategy, performance, recommendation or investment result. No
CSV, metadata sidecar or report was published.

## Immutable closure

V5 must not shorten its range, relax continuity, retry with a fallback source, re-run the Gate
download, freeze an input, implement or evaluate a candidate, select a policy, or open strict OOS
2025. Its CLI fails before constructing a network client.

The safe default remains `NEUTRAL`. No broker, order, live-trading, ML, credential, candidate
execution, selection or OOS activity is authorized. A future source decision requires a new,
separately reviewed protocol.
