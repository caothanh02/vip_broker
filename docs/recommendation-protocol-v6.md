# Recommendation research protocol V6

**Status: `source_selected_access_verification_required`.** CoinAPI is preregistered only as a
potential historical OHLCV source for `BINANCE_SPOT_BTC_USDT`, UTC 1-hour candles over
`[2019-01-01T00:00:00Z, 2022-01-01T00:00:00Z)`.

V6 has no credential loader, network client, downloader, dataset, candidate, parameter, input
lock, selection artifact or OOS authorization. Access entitlement, license/terms, provenance and
mechanical continuity must be independently verified in a later reviewed change. Source selection
must never use strategy signals, returns, accuracy, backtests, PnL or performance metrics.

Until then all actions fail closed, OOS 2025 remains sealed and the project default is `NEUTRAL`.
