# Model artifacts

Models are generated locally by `make train`; do not commit binary artifacts. Each artifact must have a metadata JSON containing its feature schema version, ordered features, validation-derived threshold, metrics and checksum.
